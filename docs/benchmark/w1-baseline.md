# W1 기준선 — NDJSON 원본

측정일: 2026-08-08
환경: macOS (darwin 23.4.0), Java 17 (Homebrew OpenJDK), Gradle 8.10.2

## 데이터셋

```
./gradlew :bench:generate --args="--count=10_000_000 --out=data/raw-10m.ndjson"
```

| 항목 | 값 |
|---|---|
| 건수 | 10,000,000 |
| 테넌트 / 디바이스 | 3 / 테넌트당 200 (총 600) |
| 키 | temperature, humidity, power_wh, running, error_code (각 200만 건) |
| 전송 주기 | 10초 |
| 시작 시각 | 2026-01-01T00:00:00Z (고정) |
| 커버 기간 | 약 9시간 |

## 결과

| 지표 | 값 |
|---|---|
| **NDJSON 크기** | **1,966,190,528 bytes (1,875 MiB)** |
| 건당 크기 | 196.6 bytes |
| 생성 시간 | 4.5s |
| 생성 처리량 | 2,237,544 pt/s |

**이 1,875 MiB 가 W3 Parquet 압축률의 분모다.**

## 값 분포 검증

압축률 측정이 의미를 가지려면 합성 데이터가 실제 센서의 압축 특성을 가져야 한다.
`SensorsTest` 가 아래를 회귀 테스트로 고정하고 있다.

| 키 | 압축 전제 | 실측 |
|---|---|---|
| temperature | 델타 인코딩 (완만한 변화) | 10초 간격 최대 변화 < 1.0℃, 고유값 287 (2.3년 표본) |
| humidity | 델타 인코딩 | 범위 52.4~87.1, 고유값 348 |
| power_wh | 비트팩킹 (정수) | 업무시간 패턴 + 0.2% 스파이크 |
| running | RLE | 4시간 런, 28시간에 전환 10회 미만 |
| error_code | 딕셔너리 | "OK" 98%, 카디널리티 5 |

온도-습도 상관계수 **r = −0.820** — 강하지만 완전하지 않다. 실제 센서와 유사.

### 수정 이력: 습도 하드 클램프 제거

최초 구현은 `Math.min(95, Math.max(20, h))` 로 잘랐다. 그 결과:

- 표본의 **3.5%가 정확히 95.0** 에 쌓임
- 95.0 이 단일 최빈값 (6,963회 vs 차순위 806회)

경계값에 표본이 몰리면 딕셔너리/RLE 가 그 값을 공짜로 먹어서 **압축률이 실제보다 좋게 나온다.**
`tanh` 기반 soft clamp 로 교체 후 최빈값 비중 3.5% → 0.57%.
회귀 방지 테스트: `SensorsTest#continuousSensorsHaveNoSpike`.

## 경고: NDJSON 대비 압축비를 그대로 발표하면 안 된다

pyarrow 로 200만 건(1.8시간치)을 zstd Parquet 으로 변환해본 예비 측정에서
NDJSON 대비 **160배**가 나왔다. 이 숫자는 세 가지 이유로 부풀려져 있다.

1. **표본 구간이 짧다** — 1.8시간이면 ts 고유값이 666개뿐이라 딕셔너리가 비현실적으로 잘 먹는다
2. **NDJSON 이 뚱뚱한 기준선이다** — 필드명과 UUID(36자)를 매 행 텍스트로 반복한다.
   회사를 설득할 때 쓸 정직한 분모는 JSON 이 아니라 **Cassandra 의 실제 디스크 사용량**이다
3. value 를 문자열로 넣은 예비 측정이었다 (ADR-0002 대상)

열별 내역에서 `value` 열만 1.1배였다 — 나머지는 전부 반복 메타데이터였다는 뜻이다.

| 열 | 압축 전 | 압축 후 | 배율 |
|---|---|---|---|
| key | 389K | 7K | 55.1x |
| type | 261K | 10K | 25.2x |
| deviceId | 1,311K | 186K | 7.1x |
| **value** | **1,160K** | **1,039K** | **1.1x** |

→ **보고용 압축비는 (a) 1년 커버 데이터셋에서, (b) Cassandra 실제 디스크 대비로 재야 한다.**
   W3 에서 Cassandra 저장 크기 추정치를 별도 산출할 것.

## W3 진입 전에 정할 것

**현재 기본 설정은 1천만 건이 9시간밖에 커버하지 못한다.** W4 쿼리 3종 중
Q2(1년 일평균), Q3(1개월 평균)를 재려면 1년을 커버하는 데이터셋이 따로 필요하다.

10초 주기 × 600 디바이스로 1년을 채우면 94억 건 — 로컬에서 불가능하다.
아래 중 하나를 골라야 한다.

| 안 | 구성 | 1년 건수 | 비고 |
|---|---|---|---|
| A | 디바이스 50, 주기 60초 | 1.31억 | 유력. NDJSON 약 26GB, Parquet 은 훨씬 작을 것 |
| B | 디바이스 600, 주기 300초 | 3.15억 | 디바이스 카디널리티 유지. 용량 부담 |
| C | 디바이스 600, 주기 10초, 30일만 | 15.5억 | 장기 쿼리 측정 불가 — 탈락 |

### 결정 (2026-08-09) — A안 채택

```
./gradlew :bench:generate --args="--days=365 --devices-per-tenant=17 --interval-seconds=60 --out=none"
```

| 항목 | 값 |
|---|---|
| 건수 | **134,028,000** |
| 테넌트 / 디바이스 | 3 / 테넌트당 17 (총 51) |
| 전송 주기 | 60초 |
| 커버 기간 | **365일** (2026-01-01T00:00:00Z 부터) |
| **NDJSON 크기** | **26,375,747,843 bytes (24.6 GiB)** |
| 건당 크기 | 196.8 bytes |
| 생성 시간 | 51.2s (2,618,803 pt/s) |

건수 대신 `--days` 로 지정한다. 디바이스 수나 주기를 건드릴 때마다 커버 기간이 조용히
어긋나는 것을 막기 위해서다. 50이 3으로 나뉘지 않아 테넌트당 17 × 3 = 51 디바이스로 갔다.

**이 24.6 GiB 파일은 디스크에 만들지 않았다.** 로컬 여유가 45 GiB 뿐이고, 어차피 이 문서와
W2 문서가 결론 낸 대로 NDJSON 은 보고용 분모가 아니다 (`--out=none` → `CountingOutputStream`).
`NdjsonBaselineTest` 가 스트리밍 카운트와 실제 파일이 바이트 단위로 같음을 고정하고 있다.

### 디바이스 600 → 51 축소의 영향

약속대로 기록한다. 스킴 C(`tenant/device/date`)의 디렉터리 수가 **219,000 → 18,615** 로 줄어
디렉터리 폭발이 원래보다 12배 완화된다. 다만 W3 프로브에서 드러났듯 **small file 문제의 지배적
변수는 디바이스 수가 아니라 파티션 granularity** 였다 —
[w3-partition-granularity.md](w3-partition-granularity.md) 참고.

적재 쪽 영향도 있다. 스킴 C 는 같은 시각에 디바이스 × 키만큼 라이터가 동시에 열리므로
600 디바이스였다면 3,000개였을 것이 255개로 줄어, 라이터 상한을 현실적인 값으로 둘 수 있다.
